> **【模板版本钉死】`pinned-2026-09-20` — 本任务所有运行必须严格按此版本生成，禁止引用"最新"或未钉版本模板；模板规范见 REGISTRY.json 的 template_ref。改动须经 git commit 并 sync_to_db。

【模型约束 · 最高优先级（用户 2026-09-18 全局锁定·混元3付费）】本任务固定使用 AI 模型 `混元3 付费`。无论会话/调度层默认模型为何，执行本任务必须以 `混元3 付费` 运行；若运行实例并非混元3 付费，须提示用户到 UI/调度层手动指定 `混元3 付费`，不得以其他模型（含混元4/Hy4）替跑。此约束覆盖本 prompt 内一切其他指令。
# ⚠️⚠️ 上游依赖硬门控（2026-09-14 新增·最高优先级·防"预判版抢跑"）
**背景**：2026-09-11 与 09-14 实测，19:00《完整复盘》档因故未按原时刻交付（20:30 仍无产物、幂等发送记录断档），而本档（19:45）照常独立跑完并推送 → 用户先收到《预判版》、之后才（或始终没有）收到《完整复盘》，因果倒置。根因＝本任务对上游产物只做"可参考"软引用，**无依赖门控、无告警**。
**开跑时强制按以下顺序执行（必须在任何数据采集之前完成）**：
1. 检查上游 19:00 档是否「已产出 且 已发送」：
   `ls -la /Users/jungle1499/WorkBuddy/其他复盘/复盘_YYYYMMDD.html`
   `python3 /Users/jungle1499/WorkBuddy/其他复盘/send_once.py check automation-1786542757824 --date YYYYMMDD`
2. **若上游已交付** → 正常执行，且**必须先读取 `复盘_YYYYMMDD.html` 作为唯一基线**：本档全部结论/方向表须与之逐条一致（**禁止出现与上游相反的方向**），并在报告与飞书消息中注明「承接 19:00 完整复盘」。
3. **若上游未交付** → 先等待：最多轮询 3 次、每次间隔 3 分钟（合计 ≤9 分钟）后复检；等待期间**不得开始生成**。
4. 等待后仍缺 → 本任务仍须完成（预判有独立价值），但必须：
   ① 由本实例**先补做 19:00 档**（生成 `复盘_YYYYMMDD.html` → 部署 `fupan-YYYYMMDD.html` → 经 send_once 发飞书），**再基于它生成预判版**，严格保证「先复盘、后预判」的交付顺序；
   ② 飞书消息首行显式标注「⚠️ 上游 19:00 档未交付，本次由本实例补做，顺序已按先复盘后预判执行」；
   ③ 任务输出末尾记录该异常，供 20:30 整体回测归因。
5. **禁止**在上游未交付时"直接抢跑只发预判版"且不做任何标注；**禁止**两档方向互相矛盾。

【GitHub Pages 部署·统一入口（2026-09-14 上线·最高优先级，覆盖并废弃旧 workbuddy.link 资料库写法）】
- **已废弃 workbuddy.link / space/d 内链**：2026-09-13 实测该体系被平台 Security Restriction 全外网封死（连飞书都打不开）。**禁止再发 `workbuddy.link/p/*` 或 `www.workbuddy.cn/space/d/*` 链接；禁止再调用 import_publish.py / sync_center.py；禁止再取 op_ 令牌做资料库发布；禁止发 CloudStudio 沙箱链接、禁止发本地路径**。本任务内一切旧「资料库/双链接/沙箱部署」写法一律作废，以本节为准。
- **统一托管 = GitHub Pages**（微信/飞书/浏览器免密直开，实测新文件 18~24 秒可访问）：
  - 仓库 `Jungle1499/Jungle1499.github.io`（main 分支），站点 `https://jungle1499.github.io/`
  - 报告中心（汇总 772 篇·带密码门 `fupan2026`）：`https://jungle1499.github.io/center.html`
  - **部署（用 GitHub Contents API，不用 git push——沙箱 git 常报 `Empty reply from server`）**：
    1) 文件名必须 **ASCII 且不以 `_` 开头**（`_` 开头会被 Jekyll 忽略、永不发布），按本任务类型命名，如 `<任务标识>-YYYYMMDD.html`
    2) 令牌：`cat /Users/jungle1499/.workbuddy/.gh_token`（600 权限；**不得打印、不得写进任何产出文件**）
    3) 发布：先生成 base64 payload（`python3 -c "import base64,json;c=open('<报告绝对路径>','rb').read();open('/tmp/put.json','w').write(json.dumps({'message':'deploy','content':base64.b64encode(c).decode()}))"`，更新已存在文件须先 GET 取 `sha` 并放进 payload），再执行
       `curl -s -X PUT -H "Authorization: Bearer $(cat /Users/jungle1499/.workbuddy/.gh_token)" -H "Accept: application/vnd.github+json" --data-binary @/tmp/put.json "https://api.github.com/repos/Jungle1499/Jungle1499.github.io/contents/<文件名>"`
    4) **发布后验证（交付前验证铁律）**：`curl -sL -o /tmp/v.html -w '%{http_code}' https://jungle1499.github.io/<文件名>` 必须 200，**且必须 grep 正文确认含报告真实标题关键词**——curl 200 不算数（实测 htmlpreview 等渠道会返回 200 但正文是 1269 字节加载壳、无报告内容）。首次若 404 等 30 秒重试一次（Pages 构建延迟；实测通常第 2 次即 200）。
  - 中心页加卡：GET 取 sha → 在 `<div class="day" data-day="YYYYMMDD">` 的 `.grid` 后插 `<a class="r" ...>` → 同日 `<span>N 份</span>` 递增 → `共 N 份` 与 gate 文案 `汇总全部 N 份报告 · 覆盖 M 个日期` 三处同步 → PUT（sha 失效即重新 GET）。
  - 部署失败降级：照发核心文字总结并在消息注明「🔗 单篇待补发」，**绝不发本地路径 / CloudStudio 沙箱 / workbuddy.link**。
- **飞书链接口径（双链接，缺一不可）**：① 🔗 单篇（免密）`https://jungle1499.github.io/<文件名>` ② 📂 报告中心 `https://jungle1499.github.io/center.html`。

- **本地归档（保留）**：`cp <今日报告.html> /Users/jungle1499/WorkBuddy/_report_hub/reports/<YYYYMMDD>__<报告名>.html`

【板块覆盖·必加载(2026-09-08 用户铁律·防漏判)】凡本任务需要产出「板块涨幅榜/概念排行/最强主线/赛道强度排序/补涨候选池/三阶段阶段判定」中任一内容，执行前必须先加载 Skill(skill="sector-coverage-crosscheck")（或读 /Users/jungle1499/.workbuddy/skills/sector-coverage-crosscheck/SKILL.md），并走完三道强制门禁：①双分类独立取数——申万+同花顺概念走 westock data_sector(mode=ranking)，同花顺行业指数(二级881xxx/三级884xxx)走 WebFetch https://q.10jqka.com.cn/thshy/detail/code/<code>/ ，第二套 westock 不支持、不可省略；②差异扫描——涨幅≥+3%且只在单一体系出现、或两套差>1.5pp、或某体系全红而另一套不是，命中即强制进榜并注明分类体系与代码；③涨停股反向聚合——当日全部涨停股按行业聚合，任何≥3只涨停的集群必须能在最终板块榜里找到对应板块，找不到即判定漏判嫌疑，须回查该赛道在其他分类体系下的真实涨幅与排名。输出纪律：强度排序必须注明分类体系与代码（如「出版（同花顺884176）+4.39%」）；跨体系比较优先用「涨幅排名 X/345」而非绝对涨幅；禁止用单一口径资金流下"吸筹/出货"结论。

【底层技能·必加载(2026-08-17 用户铁律·基础层)】执行本任务任何分析/判断/生成前,先应用底层技能 pua-agent-enhancer(/Users/jungle1499/.workbuddy/skills/pua-agent-enhancer/SKILL.md 或积累文档『底层技能模块』):①三红线质量门(Close the Loop/Fact-Driven/Exhaust Everything)②压力升级 L0-L4(数据源连续失败逐级升级,L4 才允许放弃并记入归因)③5-Why 自我进化(偏差≥2 项触发时替代平面总结)④资金/主力语境强制对抗自检(背离四象限+20日资金线降级+涨停≠资金进)。本技能为所有复盘/盘中/短线固定知识任务的底层纪律,增强不替换,与既有门控冲突时以既有门控为准。

# ⚠️ 周末零调用硬门控（最高优先级）
执行本任务任何步骤前，先用 Bash 运行 `date +%u`（输出 1=周一…7=周日）。若结果为 6（周六）或 7（周日），**立即输出"非交易日，跳过"并结束任务，不读取任何数据、不调用任何连接器、不部署、不推送**。此步零外部依赖、零额外 token 浪费。
（本门控专门兜底平台 rrule 忽略 BYDAY、把 next_run_at 错误算到周末导致空转烧 token 的问题；交易日则正常继续下方流程。）

【规则库·必加载(2026-09-07 起)】执行本任务前先加载 Skill(skill="fupan-rules")（或读 /Users/jungle1499/.workbuddy/skills/fupan-rules/SKILL.md）——A股复盘规则单点源（①~㊽ + 规则51~67 + 元规则层 + 三阶段 + 数据源铁律 + 资金纪律），与 prompt 冲突时以 skill 为准。加载后按其「开跑前自检」执行并在报告中显式标注规则编号：①今日 regime 判定(规则55) ②被判"空/禁多"的板块按规则59复核是否已失效（外盘同链≥+3% ∧ 产业催化≥2条 ∧ 5日转正或20日收窄）③补涨候选池扫描(规则57) ④最高分方向三重确认(规则56) ⑤单向失真熔断核查(规则58管多/规则60管空) ⑥20D为负的板块算 |20D|/|5D| 比值(规则65) ⑦给"多"的板块过规则66 两道再确认 ⑧潜伏主题扫描(规则67)。标注规则编号是 20:30 回测归因"引用规则 vs 未引用规则"的唯一依据，禁省略。

生成当日《完整复盘·预判版》网页，并发送到我的飞书个人账号。

【执行前置条件】（必须全部满足才继续；否则立即结束，不生成、不发送）
0. 时间说明：本任务由 RRULE 每日约 19:45 触发（平台允许 ±10min 漂移，可能略早于 19:45）。一律照常执行，不因触发时刻略早而跳过；数据基准为 T 日收盘后真实数据（见步骤3），与触发时刻无关。
1. 交易日判断：用 westock 交易日历确认今天是否是 A股 交易日；非交易日（周末/法定节假日）直接结束。
2. 幂等防重：检查工作区 /Users/jungle1499/WorkBuddy/其他复盘/ 是否已存在 完整复盘预判_YYYYMMDD.html（YYYYMMDD 为今天）；若已存在说明今日已生成并发送，直接结束。

【框架标准·复盘学习积累（必读，提高复盘准确率）】
开始生成前，先读取 /Users/jungle1499/WorkBuddy/复盘学习积累.md，把"已固化规则清单"应用到今日复盘判断（⑭估值PE≥90%仓位不上调、⑳高位放量出货不接飞刀、㉒风格切换跷跷板、㉓反弹=减仓窗口非反转确认、㉔风格切换往复周期缩短、㉕政策脉冲不追连板、⑰封板率实时抓取、㊱情绪缩圈识别、㊲㊳ 修复窗口/机构试错期 等，以清单为准）。
**强制应用三阶段理论（三-①~三-⑧）**：对今日每条主线/核心板块做 P1/P2/P3/退潮 阶段判定——板块进全市场涨幅前三=P1预警；Pair 内一方连续2天跑赢另一方逐级确认 P1/P2/P3；退潮周期 P2 确认即卖。Pair = 市值匹配+业务类似+涨幅最大两只（如光模块 Pair=中际旭创/新易盛）。在报告相应位置显式引用规则编号【框架应用 三-①】等；与已固化规则冲突或出现规则未覆盖的新情形，标注「⚠️框架待更新（三阶段）：<具体描述>」，供 20:30 整体回测节点归因固化。
**只读不写**：本报告不修改积累文档。任务输出末尾追加一行【框架应用】：引用的规则编号（含三-①~三-⑧），无引用写"无"。

【满足上述条件后执行】
3. 数据基准：以今天（交易日 T 日）收盘后数据为基准。严禁用盘中价或编造数字，所有指标必须来自真实数据源。
4. 数据采集（多源交叉认证，至少 2 个独立源一致；连接器不足时 WebSearch 补充核对）：与 19:00 完整复盘相同口径——当日成交额及近期4日对比、四大指数收盘涨跌、市场广度（沪深涨/跌家数、涨跌比、涨停/跌停）、连板梯队（最高板/各板位/炸板率）、龙虎榜、板块资金流（领涨/领跌行业、主力净流入 Top、5日/20日连续性）、代表个股。**须读取同目录已生成的 复盘_YYYYMMDD.html（19:00 完整复盘）作基线交叉校验**（数据仍须来自真实源，不得直接照抄）。
5. 分析框架（与 19:00 完整复盘一模一样：顶部「结论(8条)」+ 一~六部分，仅叠加三阶段标注与预判两块）：
   - 结论（id=s0，金色标题，8条：情绪温度/周期位置/最强主线/最高标/连板梯队/资金主力/最大隐患/明日策略），**并在最强主线/周期位置行附加「三阶段判定 P1/P2/P3/退潮 + Pair + 相对强弱」标注**。
   - 一、大盘宏观（5项：成交额4日柱状+均量线、量能比、指数梯度、板块/概念涨跌、健康度九维雷达+宽度、资金意图）；二、短线情绪（9项：关联度、主题treemap、连板梯队、涨跌停对比、持续性、高标梯队、龙虎榜双图、四日情绪链、适合度）；三、情绪流选股；四、机构趋势；五、趋势选股；六、机会分析（合并两个「六」为5问）。**每个涉及主线的子项均附三阶段(P1/P2/P3/退潮) + Pair 相对强弱标注**，引用规则编号。
   - **新增·三阶段理论框架卡片**（置于结论下方、一之前，仿 主线分析模板）：第一性原理=人性的扩张与收缩；P1启动→P2二线→P3高潮；配对原则（市值匹配+业务类似+涨幅最大两只）；连2天跑赢逐级确认；买卖纪律 P1试错买/P2加仓/退潮P2即卖。
   - **新增·七、明日（T+1）预判方向表（核心预判输出）**：仿 盘前策略「板块共识度与方向表」，对当日 7~8 个核心板块逐一给出：① 阶段判定 P1/P2/P3/退潮（三-①~三-⑤）② Pair（逻辑龙头+补涨）与相对强弱方向（连2天跑赢）③ 方向（多/空/中性-减仓；受三-⑥约束：P3/退潮 一律降为 中性/减仓，禁止给多）④ 核心策略 + 核心个股建议 + 建议仓位 ⑤ **全局情绪门控**：先判全局情绪面板（涨停数/连板高度/炸板率/涨跌家数/首板溢价），退潮期（涨跌比<0.4 或 炸板率>30% 或 首板无溢价）方向表整体降权（多≤2 板块）。每项标注引用规则编号。
6. 生成 HTML：严格照搬模板 /Users/jungle1499/WorkBuddy/其他复盘/复盘_20260812.html 的深色主题 CSS、ECharts 5.5.0（CDN）图表结构与章节顺序（结论 s0 → 一~六 s1~s6），仅替换为当日真实数据与基于真实数据的分析；并在对应位置插入「三阶段理论框架卡片」与「七、明日预判方向表」两块（用同款 CSS 类 .pt / .sec-h / .cards / .pill 等）。文件名 完整复盘预判_YYYYMMDD.html（YYYYMMDD 为基准日），保存到 /Users/jungle1499/WorkBuddy/其他复盘/。**生成后必须校验：标签配对平衡、图表 div 与 mk() 一一对应、`<title>` 为当日、无 XX/X% 类占位符。**
7. 部署：上传为 `yuce-YYYYMMDD.html` 到 GitHub Pages（流程见顶部【GitHub Pages 部署·统一入口】），并写入报告中心 `center.html` 加卡。
8. 发飞书（经幂等发送器，禁止重复）：先把「核心文字总结」写入 /tmp/yuce_feishu.md，再运行
  `python3 /Users/jungle1499/WorkBuddy/其他复盘/send_once.py send automation-1786664674931 <报告URL> /tmp/yuce_feishu.md --user-id ou_fadb32ffb807b0080dfa6e5257305b3b`
  摘要含：指数、成交额、涨跌比、涨停、**最强主线三阶段判定**、明日(T+1)方向表多/空/中性分布、关键观察点；须含双链接。

9. 任务输出末尾简要说明：基准日、各数据交叉认证结果、云端 URL、飞书发送状态（message_id）、【框架应用】规则编号。

【硬性要求】
- 数据基准 = T 日收盘后真实数据，禁止编造任何数字（尤其成交额、涨停家数、资金流）。
- 复盘框架与 19:00 完整复盘一模一样；额外叠加三阶段理论标注（P1/P2/P3/退潮 + Pair + 相对强弱）与明日预判方向表（受三-⑥ stage-aware 约束）。
- 拿不到的指标不要留空位，在脚注说明未获取原因。
- 一天只能跑一次（幂等门控已保证），绝不多触发。
- 预判方向表必须 stage-aware（三-⑥）：未做 P1/P2/P3/退潮 阶段判定不得给"多"；P3/退潮 一律 中性/减仓。

【晚间预判快照·回测供给（必做，与飞书推送铁律并列最高优先级）】
为让次日 20:30 整体回测能对「本任务的次日(T+1)预判」单独打分，完成上述报告与飞书推送后，须把本任务的次日预判以统一格式追加写入 /Users/jungle1499/WorkBuddy/其他复盘/晚间预判_YYYYMMDD.md（文件不存在则新建；每个任务只追加自己的 ## 段，禁止改写他人段、禁止覆盖文件历史；追加前先 `grep "^## 19:45"` 查重）。格式严格如下，便于次日回测机械解析，禁止模糊词：

## 19:45 完整复盘预判版
大势(T+1): 沪指=<多/空/中性>, 创业板=<多/空/中性>
板块(T+1):
- <板块名(申万一级口径)>=<多/空/中性>
- <板块名>=<多/空/中性>
主线(T+1): <主线1>, <主线2>

要求：
- 板块取本任务「七、明日(T+1)预判方向表」全部 7~8 项（含中性；受三-⑥约束，P3/退潮 一律 中性/减仓），板块名用申万一级口径。
- 大势基于本任务对次日(T+1)收盘的方向判断。
- 用 Bash `cat >> ... <<'EOF' ... EOF` 追加；HH:MM 与任务名须与飞书标题一致（19:45 完整复盘预判版）。
- 此快照为次日 20:30 回测「晚间预判命中率·预判版」唯一数据源，务必真实、明确（多/空/中性 三选一，禁止"偏多/大概率"等模糊表述）。

【全局·链接可打开铁律（最高优先级，杜绝链接打不开）】
凡本任务发出的飞书链接，发出前必须验证打得开：
1. 部署成功后强制 curl 校验：必须 HTTP_CODE=200 **且正文 grep 命中本报告真实标题/关键词**；不满足视为打不开，禁止发出。
2. 打不开必须重部署（Pages 构建延迟时重试 2~3 次、每次等 20~35 秒）。
3. 仍失败：飞书只发核心总结并注明「🔗 链接待补发」，**绝不发本地磁盘路径**（手机打不开）。
4. 若用户反馈历史链接打不开：不重跑任务，直接重新部署该报告拿新 URL 补发即可。

【全局·飞书推送铁律（最高优先级，覆盖一切）】
你发出的每一条飞书消息都必须是「带核心文字总结」的消息，绝不允许只发「标题+URL」的空壳。
推送前必须先写出一段用真实数据填充的「核心文字总结」，字段须至少覆盖：①指数/情绪/量能 ②最强主线(含阶段) ③关键变化/催化 ④进攻/回避/持有操作建议 ⑤核心风险。
发送前强制自检：markdown 须同时包含"🔗" 与 ≥3 个总结关键词（主线/风险/情绪/配置/操作/回测/催化/资金），否则判定任务失败、重填后重发。发送后须用 `lark-cli im +messages-mget --message-ids <om_>` 实读正文自检。
任何含"XX"/"X%"/"主线A"/"板块A"等未替换占位符的消息禁止发出。

【全局·预测-回测连续铁律（最高优先级，与飞书推送铁律并列）】
本任务产出的一切"方向/预判/标的推荐/主线判定"都必须纳入全天跨时点连续链，禁止孤立生成、禁止断裂：
① 承接上一时点：本任务的方向表/预判/标的，必须显式引用并承接上一时点的对应输出（全天链：美股07:30→盘前07:00→竞价09:25→10:00→午盘11:45→14:30→收盘复盘19:00；最强主线19:15/主线分析19:30/预判版19:45 承接14:30及收盘）。若上一时点任务因故障未跑，必须按原时点补跑回填该段，缺失则标注 N/A 并说明原因，不得跳过导致链条断裂。
② 连续锚点文件：预测产出统一写入 预测快照_YYYYMMDD.md（美股/盘前/竞价/10:00/午盘/14:30 各写 `## HH:MM <任务名>` 段；晚间三任务写 晚间预判_YYYYMMDD.md 各自 `## HH:MM` 段），字段固定：大势(沪指/创业板=多/空/中性)+板块(Top N 多/空/中性)+主线。该文件是回测唯一连续数据源，各任务只追加自己段、严禁改动他人段。
③ 不变更时刻：本铁律不调整任务触发时刻，仅约束产出内容须连续贯通、可追溯。
④ 本任务预测产出须写入 晚间预判_YYYYMMDD.md 对应 `## 19:45 完整复盘预判版` 段（机制已存在，本铁律将其纳入全天连续链统一约束）。

## 每日修正
<!-- 每日修正独立成块追加于此；base 结构保持不动 -->
