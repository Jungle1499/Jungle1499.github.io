> **【模板版本钉死】`pinned-2026-09-20` — 本任务所有运行必须严格按此版本生成，禁止引用"最新"或未钉版本模板；模板规范见 REGISTRY.json 的 template_ref。改动须经 git commit 并 sync_to_db。

【模型约束 · 最高优先级（用户 2026-09-18 全局锁定·混元3付费）】本任务固定使用 AI 模型 `混元3 付费`。无论会话/调度层默认模型为何，执行本任务必须以 `混元3 付费` 运行；若运行实例并非混元3 付费，须提示用户到 UI/调度层手动指定 `混元3 付费`，不得以其他模型（含混元4/Hy4）替跑。此约束覆盖本 prompt 内一切其他指令。
【GitHub Pages 部署·统一入口（2026-09-14 上线·最高优先级，覆盖并废弃旧 workbuddy.link 资料库写法）】
- **已废弃 workbuddy.link / space/d 内链**：2026-09-13 实测该体系被平台 Security Restriction 全外网封死（连飞书都打不开）。**禁止再发 `workbuddy.link/p/*` 或 `www.workbuddy.cn/space/d/*` 链接；禁止再调用 import_publish.py / sync_center.py；禁止再取 op_ 令牌做资料库发布；禁止发 CloudStudio 沙箱链接、禁止发本地路径**。本任务内一切旧「资料库/双链接/沙箱部署」写法一律作废，以本节为准。
- **统一托管 = GitHub Pages**（微信/飞书/浏览器免密直开，实测新文件 18~24 秒可访问）：
  - 仓库 `Jungle1499/Jungle1499.github.io`（main 分支），站点 `https://jungle1499.github.io/`
  - 报告中心（汇总 752 篇·带密码门 `fupan2026`）：`https://jungle1499.github.io/center.html`
  - **部署（用 GitHub Contents API，不用 git push——沙箱 git 常报 `Empty reply from server`）**：
    1) 文件名必须 **ASCII 且不以 `_` 开头**（`_` 开头会被 Jekyll 忽略、永不发布），按本任务类型命名，如 `<任务标识>-YYYYMMDD.html`
    2) 令牌：`cat /Users/jungle1499/.workbuddy/.gh_token`（600 权限；**不得打印、不得写进任何产出文件**）
    3) 发布：先生成 base64 payload（`python3 -c "import base64,json;c=open('<报告绝对路径>','rb').read();open('/tmp/put.json','w').write(json.dumps({'message':'deploy','content':base64.b64encode(c).decode()}))"`，更新已存在文件须先 GET 取 `sha` 并放进 payload），再执行
       `curl -s -X PUT -H "Authorization: Bearer $(cat /Users/jungle1499/.workbuddy/.gh_token)" -H "Accept: application/vnd.github+json" --data-binary @/tmp/put.json "https://api.github.com/repos/Jungle1499/Jungle1499.github.io/contents/<文件名>"`
    4) **发布后验证（交付前验证铁律）**：`curl -sL -o /tmp/v.html -w '%{http_code}' https://jungle1499.github.io/<文件名>` 必须 200，**且必须 grep 正文确认含报告真实标题关键词**——curl 200 不算数（实测 htmlpreview 等渠道会返回 200 但正文是 1269 字节加载壳、无报告内容）。首次若 404 等 30 秒重试一次（Pages 构建延迟）。
  - 部署失败降级：照发核心文字总结并在消息注明「🔗 单篇待补发」，**绝不发本地路径 / CloudStudio 沙箱 / workbuddy.link**。
- **飞书链接口径（双链接，缺一不可）**：① 🔗 单篇（免密）`https://jungle1499.github.io/<文件名>` ② 📂 报告中心 `https://jungle1499.github.io/center.html`。

【资料库·双链接发布铁律（2026-09-09 用户铁律·最高优先级·覆盖旧"只发中心"写法）】
本任务对外发链接必须**同时给两个链接**，缺一不可：
① **单篇报告免密链接**（形如 `https://workbuddy.link/p/<id>`，点击即看，无需登录）
② **报告中心链接**（私有·需登录）：`https://www.workbuddy.cn/space/d/bs6jVtmeSAkRNKJQ68u1iD`（点开看全部历史报告）
禁止只发中心一个链接、禁止只发单篇、禁止发本地路径、禁止发 CloudStudio 沙箱链接。

- **已弃用 CloudStudio 部署**：CloudStudio 沙箱链接已大面积失效，**禁止**新建沙箱 / 发 `*.sandbox.cloudstudio.club` / `app.workbuddy.link` 链接。本任务一律走资料库（见下）。

- **本地归档（保留）**：`cp <今日报告.html> /Users/jungle1499/WorkBuddy/_report_hub/reports/<YYYYMMDD>__<报告名>.html`

- **资料库发布（单篇免密·必做，否则用户看不到单篇）**：报告 HTML 生成后，必须导入资料库并发布为公开单篇：
  1) 调用 `connect_open_platform`（资料库场景）取得 op_ 令牌（不要外泄）；若拿不到 op_ 令牌，再回退 `connect_cloud_service` 取 JWT；
  2) 运行（WB_JWT 用上一步 JWT，报告路径用绝对路径）：
     `WB_OP_TOKEN=<op_令牌> WB_JWT=<JWT> python3 /Users/jungle1499/WorkBuddy/_report_hub/import_publish.py <今日报告绝对路径.html> --parent-id bs6jVtmeSAkRNKJQ68u1iD --title "<报告标题>"`
     （⚠️ 禁止使用资料库自带 import_html.py：其写接口用 X-Skill-Token 鉴权会失败；只允许用上面的 import_publish.py。）
     脚本打印 `{"node_block_id":...,"publish_url":...}`，**publish_url 即单篇免密链接**，务必捕获。
  3) 刷新中心索引（把新报告追加进中心并重导中心节点）：
     `WB_OP_TOKEN=<op_令牌> python3 /Users/jungle1499/WorkBuddy/_report_hub/sync_center.py`
  ⚠️ 若发布失败（拿不到 JWT / 脚本报错）：仍按下方飞书铁律发送**双链接（单篇+中心）**，并在消息末尾注明「单篇链接待 20:00 同步任务补发」；不要因此阻断任务。20:00 兜底同步任务会把今日未发布的报告补发进中心。

- **飞书消息（双链接·必做）**：消息正文须同时含 **🔗 单篇报告：<publish_url>** 与 **📂 报告中心：https://www.workbuddy.cn/space/d/bs6jVtmeSAkRNKJQ68u1iD** 两个链接。

- **链接可用性自检（必执行）**：发送前必须 curl 校验**两个**链接：
  `curl -s -o /dev/null -w '%{http_code}' --max-time 30 <publish_url>` 须 200；
  `curl -s -o /dev/null -w '%{http_code}' --max-time 30 https://www.workbuddy.cn/space/d/bs6jVtmeSAkRNKJQ68u1iD` 须 200（中心为私有页，登录态返回 200；非 200 立即重导中心）。
  任一非 200 不得发送对应链接，按「链接待补发」降级（**绝不发本地路径**）。


【板块覆盖·必加载(2026-09-08 用户铁律·防漏判)】凡本任务需要产出「板块涨幅榜/概念排行/最强主线/赛道强度排序/补涨候选池/三阶段阶段判定」中任一内容，执行前必须先加载 Skill(skill="sector-coverage-crosscheck")（或读 /Users/jungle1499/.workbuddy/skills/sector-coverage-crosscheck/SKILL.md），并走完三道强制门禁：①双分类独立取数——申万+同花顺概念走 westock data_sector(mode=ranking)，同花顺行业指数(二级881xxx/三级884xxx)走 WebFetch https://q.10jqka.com.cn/thshy/detail/code/<code>/ ，第二套 westock 不支持、不可省略；②差异扫描——涨幅≥+3%且只在单一体系出现、或两套差>1.5pp、或某体系全红而另一套不是，命中即强制进榜并注明分类体系与代码；③涨停股反向聚合——当日全部涨停股按行业聚合，任何≥3只涨停的集群必须能在最终板块榜里找到对应板块，找不到即判定漏判嫌疑，须回查该赛道在其他分类体系下的真实涨幅与排名。输出纪律：强度排序必须注明分类体系与代码（如「出版（同花顺884176）+4.39%」）；跨体系比较优先用「涨幅排名 X/345」而非绝对涨幅；禁止用单一口径资金流下"吸筹/出货"结论。

【底层技能·必加载(2026-08-17 用户铁律·基础层)】执行本任务任何分析/判断/生成前,先应用底层技能 pua-agent-enhancer(/Users/jungle1499/.workbuddy/skills/pua-agent-enhancer/SKILL.md 或积累文档『底层技能模块』):①三红线质量门(Close the Loop/Fact-Driven/Exhaust Everything)②压力升级 L0-L4(数据源连续失败逐级升级,L4 才允许放弃并记入归因)③5-Why 自我进化(偏差≥2 项触发时替代平面总结)④资金/主力语境强制对抗自检(背离四象限+20日资金线降级+涨停≠资金进)。本技能为所有复盘/盘中/短线固定知识任务的底层纪律,增强不替换,与既有门控冲突时以既有门控为准。

# ⚠️ 周末零调用硬门控（最高优先级，必须置于最前）
执行本任务任何步骤前，先用 Bash 运行 `date +%u`（输出 1=周一…7=周日）。若结果为 6（周六）或 7（周日），**立即输出"非交易日，跳过"并结束任务，不读取任何数据、不调用任何连接器、不部署、不推送**。此步零外部依赖、零额外 token 浪费。
（本门控专门兜底平台 rrule 忽略 BYDAY、把 next_run_at 错误算到周末导致空转烧 token 的问题；交易日则正常继续下方流程。）

【规则库·必加载(2026-09-07 起)】执行本任务前先加载 Skill(skill="fupan-rules")（或读 /Users/jungle1499/.workbuddy/skills/fupan-rules/SKILL.md）——A股复盘规则单点源（①~㊽ + 规则51~60 + 元规则层4条 + 三阶段 + 数据源铁律 + 资金纪律），与 prompt 冲突时以 skill 为准。加载后按其「开跑前自检5问」执行并在报告中显式标注规则编号：①今日 regime 判定(规则55) ②被判"空/禁多"的板块按规则59复核是否已失效（外盘同链≥+3% ∧ 产业催化≥2条 ∧ 5日转正或20日收窄）③补涨候选池扫描(规则57) ④最高分方向三重确认(规则56·≥85分必查) ⑤单向失真熔断核查(规则58管多/规则60管空)。标注规则编号是 20:30 回测归因"引用规则 vs 未引用规则"的唯一依据，禁省略。

【幂等哨兵铁律（2026-08-23 新增·根治平台超时重试导致重复发飞书/重复链接/重复追加文件）】
在「数据采集 / 生成 HTML / 部署」之前，先运行 Bash：
  python3 /Users/jungle1499/WorkBuddy/其他复盘/send_once.py check automation-1786542757824
- 若输出 sent=true：说明今天该任务已成功发送过（大概率为上次超时后平台自动重试），**立即结束任务**，输出「今日已发送(幂等命中)，复用链接：<返回的 link>」。**禁止**重新部署、禁止重新发飞书、禁止重新追加追踪表/预测快照。直接复用已存链接即可。
- 若输出 sent=false：正常继续下方流程。
本任务 task_id = automation-1786542757824。

生成当日《完整日复盘》网页，并发送到我的飞书个人账号。

【执行前置条件】（必须全部满足才继续；否则立即结束，不生成、不发送）
0. 时间说明：本任务由 RRULE 每日约 19:00 触发（平台允许 ±10min 漂移，可能略早于 19:00）。一律照常执行，不因触发时刻略早而跳过；数据基准为 T 日收盘后真实数据（见步骤3），与触发时刻无关。
1. 交易日判断：用 westock 交易日历确认今天是否是 A股 交易日；非交易日（周末/法定节假日）直接结束。
2. 幂等防重：检查工作区 /Users/jungle1499/WorkBuddy/其他复盘/ 是否已存在 复盘_YYYYMMDD.html（YYYYMMDD 为今天）；若已存在说明今日已生成并发送，直接结束。

【框架标准·复盘学习积累（必读，提高复盘准确率）】
开始生成前，先读取 /Users/jungle1499/WorkBuddy/复盘学习积累.md（当前基线：框架 v2.8·报告 v3.3·随机基准命中率47.5%（文档内基线更新时以文档为准）），把"已固化规则清单"应用到今日复盘判断（如 ⑭估值PE≥90%仓位不上调、⑳高位放量出货不接飞刀、㉒风格切换跷跷板、㉓反弹=减仓窗口非反转确认、㉔风格切换往复周期缩短、㉕政策脉冲不追连板、⑰封板率实时抓取、㉖-㊱ 新维度、㊲㊳ 修复窗口/机构试错期等，以清单为准）。今日盘面符合某规则特征时，在报告相应位置显式引用规则编号。

【满足上述条件后执行】
3. 数据基准：以今天（交易日 T 日）收盘后数据为基准（19:00 已收盘）。严禁用盘中价或编造数字，所有指标必须来自真实数据源。
4. 数据采集（必须多源交叉认证，至少 2 个独立数据源一致才采用；连接器不足时用 WebSearch 从公开财经网站补充核对）：
   - 当日成交额及近期 4 日对比（用 westock data_market_overview type=trade 拉真实 MONEY 值，单位亿元）、四大指数收盘涨跌（上证/深成/创业板指/科创50）。
   - 市场广度：沪深涨/跌家数、涨跌比、涨停/跌停家数（通达信口径与交易所口径如有差异，页面脚注注明）。
   - 连板梯队：最高板、各板位家数、昨日涨停/昨日连板/昨日首板指数表现、主题热度（treemap 数据）。
   - 龙虎榜：买卖前五营业部（含金额）、机构专用席位家数、沪深股通方向。
   - 板块资金流：领涨/领跌行业、主力净流入 Top、5日/20日资金连续性（注意 zljlr 单位是万元，转亿元要 /10000）。
   - 代表个股：首板/连板高标、趋势股（5日资金连续性最好板块内涨幅居前者）。
5. 分析框架（严格按《复盘思路》方法论，读取 /Users/jungle1499/Downloads/复盘思路.docx 核对章节）：
   - 结论（原"纯短复盘"8条：情绪温度/周期位置/最强主线/最高标/连板梯队/资金主力/最大隐患/明日策略），置顶，金色标题，不编号，id=s0。
   - 一、大盘宏观（5项：成交额4日柱状+10日均量线、量能比、指数梯度、板块/概念涨跌、健康度九维雷达+宽度、资金意图）。
   - 二、短线情绪（9项：关联度、主题treemap、连板梯队对数轴、涨跌停对比、持续性、高标梯队、龙虎榜买/卖双图、四日情绪链、适合度）。
   - 三、情绪流选股（选2只代表股：首板+连板高标，含盘口应对表、止盈止损）。
   - 四、机构趋势（机构参与度、资金性质、政策、流动性；5日领涨、资金连续性）。
   - 五、趋势选股（资金连续性最好板块选2只趋势股，含操作表、止盈止损）。
   - 六、机会分析（合并原文档两个"六"为5问：①事件驱动先手 ②明日潜伏方向 ③历史回看：近期主线持续性验证（用板块5日/20日资金对照）④当前阶段玩法与风险约束 ⑤明日操作清单）。
6. 生成 HTML：严格照搬模板 /Users/jungle1499/WorkBuddy/其他复盘/复盘_20260812.html 的深色主题 CSS、ECharts 5.5.0（CDN）图表结构与章节顺序（结论 s0 → 一~六 s1~s6），仅替换为当日真实数据与基于真实数据的分析。文件名 复盘_YYYYMMDD.html（YYYYMMDD 为基准日），保存到 /Users/jungle1499/WorkBuddy/其他复盘/。
7. 部署：将 HTML 复制为 /Users/jungle1499/WorkBuddy/其他复盘/dist/index.html，用 （已弃用·见顶部【资料库·双链接发布铁律】，禁止部署 CloudStudio，统一发资料库固定入口） 导入资料库发布为单篇免密链接（流程见顶部【资料库·双链接发布铁律】的 import_publish 步骤，得到 publish_url），勿再部署 CloudStudio（失败不降级：不附本地路径；飞书只发核心总结并注明"云端部署待补发"，标记待补发，网关恢复后由用户触发补部署并补发链接）。
8. 发飞书（经幂等发送器，禁止重复）：先把上述「核心文字总结」（含指数/成交额/涨跌比/涨停家数/最强主线/龙虎榜要点/明日策略，及末尾「📊 回测与自我修正将于 20:30 由《整体回测》任务统一产出并推送」）写入 /tmp/fupan_feishu.md，再运行：
  python3 /Users/jungle1499/WorkBuddy/其他复盘/send_once.py send automation-1786542757824 <publish_url> /tmp/fupan_feishu.md --user-id ou_fadb32ffb807b0080dfa6e5257305b3b
  （send_once.py 发送成功后原子写入当日状态；若今天已发过（幂等命中）自动复用已存链接并拒绝重发，无需再用裸 lark-cli 或单独 idempotency-key。）
  摘要格式：lark-cli im +messages-send --as bot --user-id ou_fadb32ffb807b0080dfa6e5257305b3b --markdown '【完整复盘 YYYY-MM-DD】已生成。\n🌙 核心结论：\n1. 情绪/指数 — 情绪温度、指数涨跌幅、量能结论\n2. 最强主线 — 主线名称（阶段/核心逻辑）\n3. 最大隐患 — 最大风险点\n\n次日策略：\n🟢 进攻方向：...\n🔴 回避方向：...\n🟡 持有核心：...\n\n龙虎榜要点：...\n三红线：Close the Loop ✅ / Fact-Driven ✅（多源偏差已标⚠️缺失标N/A）/ Exhaust Everything ✅（X/Y 核查通过）\n📊 回测与自我修正将于 20:30 由《整体回测》任务统一产出并推送。\n🔗 单篇报告：<publish_url>　📂 报告中心：https://www.workbuddy.cn/space/d/bs6jVtmeSAkRNKJQ68u1iD'。**飞书摘要必须严格按上述格式生成，禁止只发标题+URL；所有结论、策略、龙虎榜要点、三红线核查项须用本报告真实数据填充，不得留占位符。**
9. 任务输出末尾简要说明：基准日、各数据交叉认证结果、云端 URL、飞书发送状态（message_id）。

【硬性要求】
- 数据基准 = T 日收盘后真实数据，禁止编造任何数字（尤其成交额、涨停家数、资金流）。
- 拿不到的指标不要在页面留空位，在脚注说明未获取原因。
- 一天只能跑一次（幂等门控已保证），绝不多触发。
- 本报告为只读（READER），不写积累文档；回测/归因/固化由 20:30《整体回测》任务执行。

【全局·链接可打开铁律（2026-08-18 用户明确要求·最高优先级，杜绝链接打不开）】
凡本任务发出的飞书链接，发出前必须验证打得开：
1. 部署成功后强制 curl 校验：code=$(curl -s -o /tmp/link_check.html -w '%{http_code}' --max-time 30 <publish_url>)，必须 HTTP_CODE=200 且内容命中本报告标题/关键词；不满足视为打不开，禁止发出。
2. 打不开必须重部署：新建全新目录（每天独立目录，如 dist_xxx / dist_xxx_b / _c（2026-09-09 实测更正：复用同目录返回同一 sandbox，并非必然 400；本任务统一走报告中心固定目录，见顶部【报告中心】）
3. 仍失败：飞书只发核心总结并注明「🔗 链接待补发」，**绝不发本地磁盘路径**（手机打不开）；任务输出标记待补发，网关恢复后由用户触发补发。
4. 若用户反馈历史链接打不开：不重跑任务，直接重新部署该报告拿新 URL 补发即可。

【全局·飞书推送铁律（最高优先级，覆盖一切）】
你发出的每一条飞书消息都必须是「带核心文字总结」的消息，绝不允许只发「标题+URL」的空壳（严禁出现"已生成：<URL>"或只有标题与链接、无总结正文的内容）。
推送前必须先写出一段用真实数据填充的「核心文字总结」，其字段须至少覆盖：①指数/情绪/量能 ②最强主线(含阶段) ③关键变化/催化 ④进攻/回避/持有操作建议 ⑤核心风险；再拼入发送命令。
发送前强制自检：markdown 须同时包含"🔗" 与 ≥3 个总结关键词（主线/风险/情绪/配置/操作/回测/催化/资金），否则判定任务失败、重填后重发。
任何含"XX"/"X%"/"主线A"/"板块A"等未替换占位符的消息禁止发出。
【全局·飞书推送·核心看好标的追踪表（必做，与飞书推送铁律并列最高优先级）】
你发出的飞书消息，在「核心文字总结」之后、「🔗 完整报告」链接之前，必须追加一张《核心看好标的追踪表》，并同步写入累积文件，供收盘后回测任务核算准确率。
1. 取材：从本次复盘/速览报告推荐的标的中，跨行业提取「最核心看好的标的」（不限于某一行业，上限 10 只；若当日无明确看多标的则写明"无核心看多标的"并跳过本表）。
2. 表格字段（飞书 markdown 表格，列顺序固定，禁止缺列）：
   | 标的(名称+代码) | 阶段(P1/P2/P3/退潮) | 推荐原因 | 近期走势 |
   - 阶段：按三阶段框架(三-①~三-⑧)对该标的判定 P1/P2/P3/退潮，并简述依据（如"P2 后排补涨"）。
   - 推荐原因：一句话核心逻辑（催化/资金/产业/估值等真实依据）。
   - 近期走势：用真实数据填（如"10日+28%、60日-12%、今日+3.7%"），禁止占位符。
3. 该表只进飞书消息、不进 HTML 报告正文（报告照旧生成）。
4. 同步累积：把同一张表（四列 + 推荐方向=多 + 推荐时刻）以 `### YYYY-MM-DD HH:MM <任务名>` 为小标题，追加写入 /Users/jungle1499/WorkBuddy/标的推荐追踪表.md（文件不存在则新建；只追加不覆盖历史）。推荐时刻取本次真实触发时刻（格式 HH:MM，从任务上下文取）。
5. 发送前自检：表格行数≥1（或无标的时写明"无"）且含「阶段」与「推荐原因」两列标题；表内不得出现 XX/X%/主线A 等未替换占位符。

## 每日修正
<!-- 每日修正独立成块追加于此；base 结构保持不动 -->
