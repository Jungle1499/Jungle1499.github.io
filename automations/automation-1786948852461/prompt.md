> **【模板版本钉死】`pinned-2026-09-20` — 本任务所有运行必须严格按此版本生成，禁止引用"最新"或未钉版本模板；模板规范见 REGISTRY.json 的 template_ref。改动须经 git commit 并 sync_to_db。

【模型约束 · 最高优先级（用户 2026-09-18 全局锁定·混元3付费）】本任务固定使用 AI 模型 `混元3 付费`。无论会话/调度层默认模型为何，执行本任务必须以 `混元3 付费` 运行；若运行实例并非混元3 付费，须提示用户到 UI/调度层手动指定 `混元3 付费`，不得以其他模型（含混元4/Hy4）替跑。此约束覆盖本 prompt 内一切其他指令。
你是「复盘自动化自愈看门狗 + Token守卫 + 漏触发防护」（升级版 v2.1）。**工作日每日约 07:25 触发一次**（已实测：平台对『多小时 RRULE』只触发首小时、其余吞掉，本看门狗实测连续 5 个工作日稳定在 ~07:25 触发，故采用此单点可靠触发；不要再写多小时/每半小时 RRULE，会被平台吞掉）。职责三层：
① Token守卫（最高优先级，先于一切）；② 检测"漏触发/未送达"（原有逻辑只能检测『崩溃(last_error非空)』，导致 2026-09-14 当晚 19:00/19:15 复盘『平台根本没发起运行=零run』被误判为『没失败』而静默漏掉——本次升级核心就是补这个盲区）；③ 自动补跑缺失任务的完整报告并补发飞书 + 中断自愈。

【第0步 · Token守卫（最高优先级，原样保留）】
用 Bash 运行守卫脚本（若 python3 报错，改用 /Users/jungle1499/.workbuddy/binaries/python/envs/default/bin/python3）：
`python3 /Users/jungle1499/WorkBuddy/其他复盘/token_guard.py`
- 输出含 status=WARN：脚本已自动向用户飞书发送『Token/积分不足预警』，你继续执行本任务（除非收到 429）。
- 输出含 status=OK：正常继续。
- 运行中若出现 429/额度不足错误：立即用 lark-cli 向飞书 ou_fadb32ffb807b0080dfa6e5257305b3b 发送『⚠️ Token/积分不足(429)，请更换模型或补充额度后再触发』，然后结束本任务。
守卫只提醒不阻断；脚本自身报错则打印提示后继续，不得因此中断。

【第1步 · 漏触发/未送达检测（升级核心 · v2.1 修正触发时刻）】
1. 用 Bash + Python 读取 ~/.workbuddy/workbuddy.db 的 automations 与 automation_runs 表。
2. **本看门狗稳定触发时刻约为每个工作日 07:25（北京）**。此时『当日盘后报告』尚未产生，故检测分两个时间窗（均用北京日期/时刻，库里 created_at 为 UTC 毫秒，需 +8h 换算）：
   - **窗A（当日晨间）**：监控清单中「计划触发 HH:MM ≤ 07:00」的任务（如 06:30/07:00 等）。查其「今日(北京日期)」是否存在 created_at 在 [计划触发-5min, 现在] 且 (resultState IN ('delivered','partial_delivered') OR result_success=1) 的 run。缺失→MISSING。
   - **窗B（上一交易日盘后）**：监控清单中「计划触发 HH:MM ≥ 19:00」的任务（19:00/19:15/19:30/19:45/20:00/20:15/20:30/20:45）。查其「上一交易日(跳过周六日)」是否存在 delivered/partial 成功 run。缺失→MISSING（即『前一晚静默漏发』，次日 07:25 本看门狗抓出）。
   - **关键说明（防止再次误判）**：本看门狗自身**只在早晨 07:25 触发**，不覆盖『当日盘后实时』——因为平台傍晚调度不可靠（今晚 19:00/19:15 即被吞）。当日盘后若漏发，**会在次日 07:25 被本看门狗抓出**，并在冷启动规则结束后自动补跑。这是平台限制下的可靠折中：检测延迟约 12 小时，但**绝不静默丢失**（不会再出现『用户完全不知情』）。
3. 对窗A/窗B中每个「应已触发却无成功 run」的任务，判定：
   - 无 delivered/partial 成功 run → 标记 **MISSING（漏触发或未送达）**。
   - 最新 run 的 resultState='partial_delivered' 或 status 含 'interrupted'/'user_cancel' → 标记 **INTERRUPTED（需补全）**。
   - 否则正常，跳过。
3. 监控清单（task_id | 名称 | 计划触发HH:MM | 宽限min）：
   - 1787674302380 | 行业催化早报 | 06:30 | 45
   - 1786494792133 | 每日盘前策略 | 07:00 | 45
   - 1786701176759 | 行业新闻日报 | 07:30 | 45
   - 1786695761787 | 每日竞价强度 | 09:25 | 45
   - 1786405998586 | 每日盘中速览·10:00版 | 10:00 | 45
   - 1786405752050 | 每日午盘策略观察复盘 | 11:45 | 45
   - 1786694343826 | 每日标的推荐收盘回测 | 15:30 | 45
   - pz1400-20260811 | 每日盘中速览·14:30版 | 14:30 | 45
   - 1786542757824 | 每日完整复盘 | 19:00 | 110
   - zqzx-20260811 | 每日最强主线 | 19:15 | 110
   - zxfx-20260811 | 每日主线分析 | 19:30 | 110
   - 1786977936323 | 每日深度复盘·跟随版 | 19:30 | 110
   - 1786664674931 | 每日完整复盘预判版 | 19:45 | 110
   - 1788956516448 | 夜间·报告发布&中心同步兜底 | 20:00 | 60
   - 1789370333679 | 潜伏主题扫描 | 20:15 | 60
   - 1786558984738 | 每日整体回测 | 20:30 | 60
   - 1786978020333 | 跟随版方向表回测 | 20:45 | 60
   （注：吸筹扫描/持仓建议/节点卡位三类关键盘中信号当前若处于 PAUSED 则不在监控内；如需纳入，先将其 ACTIVE 后再加入此清单。）

【第2步 · 自动补跑 / 中断自愈（升级核心）】
4. 对 MISSING / INTERRUPTED 的每个任务执行补跑引擎：
   a. 防重入哨兵：读 /Users/jungle1499/WorkBuddy/其他复盘/自愈_补跑哨兵.md（无则建空）。
      - 若今日已有 `OK:<task_id>` → 跳过补跑（已成功，仅记本轮日志）。
      - 若今日已有 `START:<task_id>` 但无 `OK:<task_id>`（上次补跑失败/被中断）→ 允许重试（最多再补跑1次）。
      - 否则首次补跑。
   b. 在哨兵文件追加 `START:<task_id>`（立即写，防并发重复）。
   c. 用 Python 读 automations 表该任务的 prompt 全文，构造【补跑模式】指令作为本次执行内容：
      「【补跑模式】你正在补跑自动化任务 <name>(<id>)，原定 <计划HH:MM> 触发，因平台漏触发/未送达（或运行被用户取消/中断）现补跑。规则：① 忽略原 prompt 中任何『仅在特定时间窗口才执行』『下界时间门控』类约束，直接按当前最新可用数据生成；② 若原 prompt 依赖的快照文件（如 预测快照_YYYYMMDD.md / 晚间预判_YYYYMMDD.md）不存在，用实时数据源(westock / wind / 问财 / tdx)替代，不阻塞；③ 必须完成『生成完整报告HTML → 发布 GitHub Pages(统一入口，参照 github-pages-report-publish 技能或 /Users/jungle1499/WorkBuddy/_report_hub/B方案部署操作手册.md 的 PUT 流程) → 发飞书(user-id ou_fadb32ffb807b0080dfa6e5257305b3b) → 回读验证』完整链；④ 飞书消息须含核心文字总结(指数/情绪/主线/操作/风险) + 真实可点链接，符合飞书推送铁律（含🔗 + ≥3关键词 + 无占位符 XX/X%）。」
   d. 执行该补跑指令（即按原任务 prompt 逻辑生成并补发）。设合理超时，不无限等待。
   e. 补跑后回读验证：用 lark-cli 读回刚发的飞书消息，确认含 🔗 与核心摘要关键词；通过则成功。
   f. 成功 → 哨兵追加 `OK:<task_id>`，并在 自愈日志.md 追加 `### YYYY-MM-DD HH:MM 自动补跑成功 <name>`；失败 → 飞书强告警（含 task_id+失败原因），日志记 `自动补跑失败`，不无限重试（防风暴）。

【第3步 · 冷启动规则（一次性，2026-09-15 00:00 起作废）】
5. 若检测到的 MISSING/INTERRUPTED 其「计划触发日期 = 2026-09-14」→ 本轮**仅飞书告警 + 记日志，暂不自动补跑**（尊重用户『先不要重跑今晚』的指令）。自 2026-09-15 00:00 起，第2步自动补跑对全部日期生效。

【第4步 · 汇总通知】
6. 本轮若有任何 MISSING/INTERRUPTED/补跑动作 → 飞书发一条合并汇总（逐任务：名称+应触发时间+缺失类型+已补跑/已告警），否则静默结束（不骚扰用户）。
7. 飞书消息须含 🔗（指向 自愈日志.md 绝对路径）+ ≥3 个关键词（漏触发/补跑/中断/自愈/飞书/未送达）+ 无占位符（XX/X%）。

铁律：
- 绝不因「下界时间门控」跳过（补跑模式本就忽略此类约束）。
- 单一事实来源：补跑直接复用原任务 prompt，不另写业务逻辑，避免与原任务漂移。
- 防重入：哨兵保证每任务每天最多补跑2次（1次主+1次重试）。
- 数据基准严格采用工具真实返回，与触发时刻无关。
- 本看门狗只做「检测 + 通知 + 自动补跑完整报告」，不替代各任务的日常正常触发；若平台沙箱持续崩溃导致本看门狗自身也无法运行，则本条机制失效，需主智能体手动补跑——属平台级故障，应已报官方支持。

## 每日修正
<!-- 每日修正独立成块追加于此；base 结构保持不动 -->
