【全局·自动化版本管理自检任务】

本任务由 RRULE 每日约 22:00 触发（平台允许 ±10min 漂移，可能略早于 22:00）。一律照常执行，不因触发时刻略早而跳过；数据基准为 automation_registry 仓库当前文件状态，与触发时刻无关。

目标：保证所有固定自动化任务的"实际状态"与版本库（单一事实源）一致，发现漂移立即修复并飞书告警。

步骤：
1. 进入仓库：`cd /Users/jungle1499/WorkBuddy/automation_registry`
2. 运行 `python3 scripts/check_drift.py`，完整读取输出。
3. 若输出显示 0 漂移（prompt 文件=DB、模型全 hy3、模板全钉死 pinned-2026-09-20）：仅记录"今日一致"，默认不推送飞书（避免打扰）。
4. 若发现漂移（模型≠hy3 / 模板未钉死 / prompt 文件与 DB 不一致 / 任务状态异常）：
   a. 立即以文件为权威源执行 `python3 scripts/sync_to_db.py --all` 把钉死版本重新同步进 DB；
   b. 复跑 `python3 scripts/check_drift.py` 确认归零；
   c. 飞书推送告警（user-id `ou_fadb32ffb807b0080dfa6e5257305b3b`），核心文字总结须含：①哪几个任务漂移 ②漂移类型（模型/模板/内容）③已自动修正结果 ④核心风险（若复跑仍非零需用户介入）；必须含 🔗 与 ≥3 个关键词（漂移/修正/模型/模板/钉死），无未替换占位符。
5. 飞书推送铁律：推送前必产核心文字总结，自检 markdown 含 🔗 与 ≥3 关键词，否则判失败重填重发；含 XX/X%/主线A 等未替换占位符禁止发出。

注意：本任务只做"漂移检测+从文件自愈"，绝不改动 base 模板正文；如需升级模板版本，必须等用户明确指令。模型统一要求：本任务涉及的所有固定自动化 model_id 必须为 hy3（混元3付费版），发现非 hy3 一律改回 hy3 后同步。