【三阶段理论 v3.0 · 全局口径覆盖（2026-09-21 用户拍板·最高优先级，覆盖并废弃本 prompt 内一切旧三阶段写法；不影响周末/时间/幂等门控，门控照常优先）】
凡本任务涉及三阶段 / P1/P2/P3 阶段判定，一律按**三阶段理论 v3.0**（原 v4.17.0 状态机）执行，**两层结构**：
① **第一层·板块筛**：板块进全市场涨幅前三 → 列为 P1 候选（**只筛选，不定级**）。
② **第二层·Pair 定级**（阶段判定唯一依据）：板块内取龙头/补涨 Pair，按 v3.0 状态机判定 —— P1 触发 = 任一只当日涨幅 **≥5%**；确认走 **D0/D1/D2 三天观察窗四路径**，并按 D0~D2 累计涨幅最高者**定死 leader**；P2/P3 用**回溯前两日 2-of-3**（最快 T+1，允许中间插一天）；中断**仅 P1/P2 阶段且只看龙头单日跌 ≥6%**（补涨跌不算）；**P3 确认 = 持有**（非清仓），龙头开跌或双双跌才 W 结束；状态可回退（P3 中补涨强→回 P2，P2 中龙头强→回 P1）。
⚠️ 本 prompt 内原有的「连 2 天跑赢」「板块进涨幅前三 = P1 预警」「P3 确认即清仓」「7 状态」等旧口径一律作废，以本节为准。

> **【模板版本钉死】`pinned-2026-09-20` — 本任务所有运行必须严格按此版本生成，禁止引用"最新"或未钉版本模板；模板规范见 REGISTRY.json 的 template_ref。改动须经 git commit 并 sync_to_db。

【模型约束 · 最高优先级（用户 2026-09-18 全局锁定·混元3付费）】本任务固定使用 AI 模型 `混元3 付费`。无论会话/调度层默认模型为何，执行本任务必须以 `混元3 付费` 运行；若运行实例并非混元3 付费，须提示用户到 UI/调度层手动指定 `混元3 付费`，不得以其他模型（含混元4/Hy4）替跑。此约束覆盖本 prompt 内一切其他指令。
【任务】运行「🔭 潜伏主题扫描 · Discovery Gate」（复盘规则库规则67）。一句话回答：「今天有没有潜伏候选？」

【为什么】盘前/盘中扫描取的都是已启动者，催化日历孤立不参与选板 → 「催化在前+资金未到」的主题永远进不了流程。补这个缺口。必须覆盖两条通道：①日历型（事件库）②价格型（价格指标创新高，无固定日期，如运价/制冷剂报价）。

【判定口径 · 2026-09-16 用户改定，硬性】
- 潜伏 = 未来 **10 个交易日**内有催化 ＋ 该概念资金净流入处于**全市场 ≤40 分位** ＋ 今日涨幅 <1.5%
- 资金口径：**优先自算真 10 日**（需 ≥10 份快照），不足时回退 `mainNetInflow5d`（脚本已自动处理，并在报告头部标注当前口径）
- 半启动 = 资金分位 >60 且今日 <2%；已启动 = 今日 ≥2%
- 必须用全市场分位数，禁用绝对值；日历型剔除成分 <8 的迷你概念；theme 匹配 token 必须 ≥3 字符

【步骤】
1. 交易日门控：`date +%u` 为 6/7 则输出「非交易日，跳过」并结束。
2. 幂等门控：若 `研究/潜伏主题扫描_YYYYMMDD.md` 已存在且非空，跳过第 3~5 步直接进第 6 步。
3. 取概念快照：ToolSearch 加载 `mcp__westock-mcp__data_sector`，DeferExecuteTool 调 {"mode":"ranking","kind":"concept","type":"changePct","limit":1000}。若落盘为 tool-result 文件须读完整 JSON。原样存 `研究/概念排名快照_YYYYMMDD.json`（**这份是攒真 10 日资金的原料，每天必须存**）。
4. 更新价格型催化清单 `研究/价格型催化清单.json`：`latest.value` 为空或 date 早于 3 个交易日的条目用 WebSearch 查最新值回写；查不到保留原值注「未更新」，**禁止编造数值**。
5. 跑扫描器：`/Users/jungle1499/.workbuddy/binaries/python/versions/3.13.12/bin/python3 研究/discovery_scan.py > 研究/潜伏主题扫描_YYYYMMDD.md`（同时产出 `潜伏候选_YYYYMMDD.json`）。
6. **跑连续追踪（回测账本，必做）**：`python3 研究/discovery_track.py`。它把候选登记进 `研究/潜伏候选账本.json`（只增不删的时间序列），并按下列口径判定：
   - **命中** = 催化日 **±1 个交易日**内，任一日板块涨幅 ≥2% 或 资金分位 ≥60
   - **失效** = 今日已过「催化日+1 交易日」且窗口内从未触发
   - 输出 `研究/潜伏主题追踪_YYYYMMDD.md`（T+0/T+1…矩阵）。已结案的须报命中/失效/跟踪中计数与命中率。
7. 写聚焦池：在 `交易/吸筹扫描_聚焦池.md` 末尾追加/替换 `## 🔭 潜伏候选（YYYY-MM-DD）` 节，格式 `- 板块名 ← 【日历型/价格型】催化（T+N 交易日）｜资金分位｜催化窗口`。无候选写「本日无潜伏候选」。
8. 发飞书：`lark-cli im +messages-send --as bot --user-id ou_fadb32ffb807b0080dfa6e5257305b3b --markdown "…" --idempotency-key discovery-YYYYMMDD`（PATH 含 `/Users/jungle1499/.workbuddy/binaries/node/cli-connector-packages/bin` 与 `.../versions/22.22.2/bin`）。必须含：①候选清单（板块名+通道+催化与剩余交易日+资金分位）②≥3 个关键词 ③🔗 与免责 ④无占位符 ⑤**一行白话术语**（T+N＝往后数 N 个交易日；资金分位＝全市场概念按资金从少到多排队的百分位，越低越没人关注）。无候选也要发。
9. 写 memory：向 `研究/.workbuddy/memory/YYYY-MM-DD.md` 追加当日结论（候选数 + 命中/失效/跟踪中计数 + 与上日对比）。

【铁律】
- **不得用三阶段替代本环节**（三阶段答「有多强」，本环节答「有没有」）。分工必须写进报告：规则57 在已活跃板块找补涨；吸筹扫描＝**P1 预警器**（找出「资金已进、价格未大涨」的标的）；本环节在全市场找尚未启动的主题。
- 🔴 **禁止假设用户熟悉术语**。报告与飞书必须自带白话解释；`discovery_scan.py` 内置 GLOSSARY「📖 术语速查」不得删除。
- 🔴 **用词**：报告/表格中一律用「**关联板块**」，**禁用「命中概念」**（用户曾误解"命中"＝选中好股，实际只是催化关键词与概念名匹配上）。
- 🔴 **「代表标的」不得误导**：`leader` 是「当日涨幅最大成分股」（常是涨停股），**不是龙头、不是买点**，必须原样标注并加⚠️。改进方案（近2日涨幅70分＋近5日均涨幅30分）已记为**回测观察点 #1**（`研究/回测观察点.md`），**回测前不执行**；前置阻塞是成分股取数接口报错。
- 数据取不到须明说原因并给修复建议，**禁止静默跳过、禁止编造候选或编造价格**。
- 补跑/回测必须用同日快照：`discovery_scan.py --as-of YYYY-MM-DD` 会自动取 `概念排名快照_YYYYMMDD.json`，找不到会打印失真警告。

## 每日修正
<!-- 每日修正独立成块追加于此；base 结构保持不动 -->